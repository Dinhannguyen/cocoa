local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local VirtualInputManager = game:GetService("VirtualInputManager")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer

-- [[ CẤU HÌNH HOP SERVER BROWSER ]] --
math.randomseed(tick() + LocalPlayer.UserId)
local targetCountMin = 1
local targetCountMax = 4
local targetRegion = "Singapore"
local isHopping = false
local startTime = tick()
local lastTriedJob = nil

-- [[ CẤU HÌNH AFK MONITOR ]] --
local CHECK_INTERVAL = 5         -- kiểm tra mỗi 5 giây
local MAX_IDLE_TIME = 300        -- 5 phút (300 giây)
local folderName = "mm"          -- Tên folder chứa file server
local lastPosition = nil
local idleTime = 0

-- [[ HÀM THÔNG BÁO ]] --
local function notify(title, text, duration)
    pcall(function()
        game.StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = duration or 5
        })
    end)
end

-- [[ HÀM KIỂM TRA SERVER CÓ FILE BẢO KÊ KHÔNG ]] --
local function isCurrentServerSpecial()
    if not isfolder(folderName) then return false end
    
    local files = listfiles(folderName)
    for _, file in ipairs(files) do
        local success, content = pcall(readfile, file)
        if success then
            local data = HttpService:JSONDecode(content)
            -- Nếu JobID trong file trùng với JobID server hiện tại
            if data.jobId == game.JobId then
                return true
            end
        end
    end
    return false
end

-- [[ LOGIC HOP BẰNG SERVER BROWSER ]] --
local function ClickRefresh()
    pcall(function()
        local gui = LocalPlayer.PlayerGui:FindFirstChild("ServerBrowser")
        if not gui then return end
        local frame = gui:FindFirstChild("Frame")
        if not frame then return end
  
        local refreshBtn = frame:FindFirstChild("Refresh")
        if refreshBtn and refreshBtn.Visible then
            local x = refreshBtn.AbsolutePosition.X + (refreshBtn.AbsoluteSize.X / 2)
            local y = refreshBtn.AbsolutePosition.Y + (refreshBtn.AbsoluteSize.Y / 2) + 58
            VirtualInputManager:SendMouseButtonEvent(x, y, 0, true, game, 1)
            task.wait(0.05)
            VirtualInputManager:SendMouseButtonEvent(x, y, 0, false, game, 1)
        end
    end)
end

local function HopWithServerBrowser(jobId)
    local remote = ReplicatedStorage:FindFirstChild("__ServerBrowser")
    if remote then
        pcall(function()
            remote:InvokeServer("teleport", jobId)
        end)
    end
end

local function FilterAndJump(serverList)
    if isHopping or type(serverList) ~= "table" then return end

    local candidates = {}

    for jobId, info in pairs(serverList) do
        if type(info) == "table" then
            local pCount = tonumber(info.Count) or 0
            local pRegion = tostring(info.Region) or ""

            if jobId ~= game.JobId
            and string.find(pRegion, targetRegion)
            and (pCount >= targetCountMin and pCount <= targetCountMax) then
                table.insert(candidates, jobId)
            end
        end
    end

    if #candidates > 0 then
        local chosenJob = candidates[math.random(1, #candidates)]

        isHopping = true
        lastTriedJob = chosenJob

        notify("Hop Server", "Đã tìm thấy server! Đang tham gia...", 5)
        HopWithServerBrowser(chosenJob)

        -- retry nếu fail → chọn server khác
        task.spawn(function()
            task.wait(5)
            if game.JobId == lastTriedJob then
                isHopping = false

                if #candidates > 1 then
                    local newJob
                    repeat
                        newJob = candidates[math.random(1, #candidates)]
                    until newJob ~= lastTriedJob

                    HopWithServerBrowser(newJob)
                else
                    HopWithServerBrowser(lastTriedJob)
                end
            end
        end)
    end
end

-- hook server browser
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local response = oldNamecall(self, ...)

    if not isHopping
    and (method == "InvokeServer" or method == "FireServer")
    and tostring(self.Name) == "__ServerBrowser" then
        if type(response) == "table" then
            task.defer(function()
                FilterAndJump(response)
            end)
        end
    end

    return response
end)

setreadonly(mt, true)

local function StartHopProcess()
    if isHopping then return end
    
    local ui = LocalPlayer:WaitForChild("PlayerGui"):WaitForChild("ServerBrowser", 5)
    if ui then
        ui.Enabled = true
        local frame = ui:FindFirstChild("Frame")
        if frame then 
            frame.Visible = true 

            local filters = frame:FindFirstChild("Filters")
            if filters and filters:FindFirstChild("SearchRegion") and filters.SearchRegion:FindFirstChild("TextBox") then
                filters.SearchRegion.TextBox.Text = targetRegion
            end
        end
    else
        notify("Lỗi", "Không tìm thấy UI ServerBrowser!", 5)
        return
    end
    
    startTime = tick()

    task.spawn(function()
        while not isHopping do
            pcall(function()
                local remote = ReplicatedStorage:FindFirstChild("__ServerBrowser")
                if remote then
                    remote:InvokeServer(1, targetRegion)
                end
            end)

            task.wait(3)

            if not isHopping then
                if (tick() - startTime) >= 10 then
                    pcall(function()
                        if ui then
                            ui.Enabled = false
                            task.wait(0.5)
                            ui.Enabled = true

                            if ui:FindFirstChild("Frame") then
                                ui.Frame.Visible = true
                            end
                            
                            local remote = ReplicatedStorage:FindFirstChild("__ServerBrowser")
                            if remote then
                                remote:InvokeServer(1, targetRegion)
                            end
                        end
                    end)

                    startTime = tick()
                else
                    if UserInputService.WindowFocused then
                        ClickRefresh()
                    end
                end
            end
        end
    end)
end

-- [[ LOGIC AFK MONITOR ]] --
local function monitorIdle()
    while true do
        task.wait(CHECK_INTERVAL)

        -- Bỏ qua nếu đang trong quá trình hop
        if isHopping then continue end

        -- [[ CHECK FILE BẢO KÊ ]] --
        if isCurrentServerSpecial() then
            idleTime = 0
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                lastPosition = LocalPlayer.Character.HumanoidRootPart.Position
            end
            continue 
        end

        local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local root = character:FindFirstChild("HumanoidRootPart")
        if root then
            local currentPos = root.Position
            if lastPosition and (currentPos - lastPosition).magnitude < 1 then
                idleTime = idleTime + CHECK_INTERVAL
            else
                idleTime = 0
                lastPosition = currentPos
            end
            
            if idleTime >= MAX_IDLE_TIME then
                notify("AFK", "Đứng yên quá lâu! Đang khởi động Server Browser...", 5)
                idleTime = 0
                lastPosition = root.Position
                
                -- Kích hoạt quá trình hop server browser
                StartHopProcess()
            end
        end
    end
end

print("✅ AFK Monitor loaded! (Mode: ServerBrowser Hop + MM Safe Check)")
monitorIdle()
